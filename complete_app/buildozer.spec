# Permissions
android.permissions = CALL_PHONE, INTERNET

# Android API
android.api = 31  # or your desired level

# Requirements
requirements = kivy, jnius, android

# Entry point (if using Kivy app)
entrypoint = main.py
